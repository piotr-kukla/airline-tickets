apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "airline.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "airline.name" . }}
    helm.sh/chart: {{ include "airline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ .Values.airline.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "airline.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "airline.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.airline.image.repository }}:{{ .Values.airline.image.tag }}"
          imagePullPolicy: {{ .Values.airline.image.pullPolicy }}
          envFrom:
          - configMapRef:
              name: {{ include "airline.fullname" . }}-config
          - secretRef:
              name: {{ include "airline.fullname" . }}-secret
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            initialDelaySeconds: {{ .Values.airline.liveness_initial_delay }}
            httpGet:
              path: /
              port: http
          readinessProbe:
            initialDelaySeconds: {{ .Values.airline.readiness_initial_delay }}
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
