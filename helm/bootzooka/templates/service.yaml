apiVersion: v1
kind: Service
metadata:
  name: {{ include "airline.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "airline.name" . }}
    helm.sh/chart: {{ include "airline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.airline.service.type }}
  ports:
    - port: {{ .Values.airline.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "airline.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
